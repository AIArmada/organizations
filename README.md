# AIArmada Organizations

Reusable organization tenancy for Laravel applications.

The package provides the `Organization` aggregate, polymorphic membership
integration through `aiarmada/membership`, lifecycle and ownership actions, and
contracts for application-specific authorization, presentation, profile
extensions, lifecycle hooks, and current-organization resolution.

The package does not own event organizers, addresses, media, billing,
moderation, or Filament resources.
